apiVersion: v1
kind: Pod
metadata:
  name: 3cc3ntric-pod
  namespace: 3cc3ntric-dev
spec:
  imagePullSecrets:
    - name: ghcr-pull-secret
  containers:
    - name: 3cc3ntric-container
      image: ${IMAGE}
      ports:
        - containerPort: 8080
